server:
  port: ${SERVER_PORT:8080}

spring:
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://ep-little-rain-anufkqgy.c-6.us-east-1.aws.neon.tech/neondb?sslmode=require}
    username: ${DATABASE_USERNAME:neondb_owner}
    password: ${DATABASE_PASSWORD:npg_8msaqdTNXE4r}
    driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  
  sql:
    init:
      mode: always

# Supabase configuration (alternative)
# spring:
#   datasource:
#     url: ${SUPABASE_DB_URL:jdbc:postgresql://postgres:${SUPABASE_DB_PASSWORD}@db.########.supabase.co:5432/postgres}
#     username: ${SUPABASE_DB_USERNAME:postgres}
#     password: ${SUPABASE_DB_PASSWORD}
#     driver-class-name: org.postgresql.Driver

app:
  jwt:
    secret: ${JWT_SECRET:mySecretKey123456789012345678901234567890}
    expiration-ms: ${JWT_EXPIRATION_MS:86400000}
